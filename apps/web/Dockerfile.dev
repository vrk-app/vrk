FROM node:24.14.1-alpine

ENV NEXT_TELEMETRY_DISABLED=1

WORKDIR /workspace

RUN corepack enable && corepack prepare pnpm@10.33.0 --activate

EXPOSE 3000

CMD ["pnpm", "--filter", "@vrk/web", "dev", "--hostname", "0.0.0.0", "--port", "3000"]
